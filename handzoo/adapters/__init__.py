"""Adapters. The engine in `handzoo.core` knows nothing about any of these.

M0 ships CLI adapters; MCP and an HTTP/UI adapter come later against the same engine. The
dependency runs one way only, and `tests/test_architecture.py` enforces it.
"""
